# =============================================================================
# GRUB configuration for 467OS
# =============================================================================
# GRUB loads this menu. "multiboot" loads our kernel.bin; "boot" runs it.
# =============================================================================

set timeout=1
set default=0

menuentry "467OS - Minimal Educational OS" {
    multiboot /boot/kernel.bin
    boot
}
